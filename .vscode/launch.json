{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Run Script",
      "type": "debugpy",  // 
      "request": "launch",
      "program": "${file}",  // runs the currently open file
      "console": "integratedTerminal",
      "justMyCode": false
    }
  ]
}
